import React from 'react';

const OurVisionCircle = (props) => {
    return (
        <div className={"ourVisionCircle themeDarkFontColor"}>
            {props.children}
        </div>
    );
};

export default OurVisionCircle;